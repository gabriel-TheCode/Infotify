<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        The only XML theme left. It exists solely for the system splash screen, which is
        drawn by the platform before any Compose code runs.
    -->
    <style name="Theme.Infotify.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/avd_splash_icon</item>
        <!--
            Matches the drawable. The platform caps this at 1000ms; going near that cap
            would turn a brand moment into a wait.
        -->
        <item name="windowSplashScreenAnimationDuration">520</item>
        <item name="postSplashScreenTheme">@style/Theme.Infotify</item>
    </style>

    <style name="Theme.Infotify" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/splash_background</item>
    </style>
</resources>
